<Window x:Class="SightKeeper.UI.WPF.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SightKeeper"
        xmlns:viewModels="clr-namespace:SightKeeper.UI.WPF.ViewModels.Windows"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:MainWindowVM/>
    </Window.DataContext>
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <ListBox Name="SideMenu" Grid.Column="0" ItemsSource="{Binding SideMenuItems, Mode=OneTime}"
                 SelectedItem="{Binding SelectedSideMenuItem, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name, Mode=OneTime}"
                               Style="{StaticResource SideMenuTextBlock}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <ContentControl Name="SideMenuContent" Grid.Column="1"
                        Content="{Binding SelectedSideMenuItem.Content, UpdateSourceTrigger=PropertyChanged}"/>
        
        <Border Grid.Column="0" Grid.ColumnSpan="2" Background="#50000000" Visibility="{Binding PopupVisibility, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Border Background="White" VerticalAlignment="Center" HorizontalAlignment="Center">
                <ContentControl Content="{Binding PopupToShow, UpdateSourceTrigger=PropertyChanged}"/>
            </Border>
        </Border>
        
    </Grid>
</Window>
