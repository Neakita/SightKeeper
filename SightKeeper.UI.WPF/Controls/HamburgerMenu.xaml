<UserControl x:Class="SightKeeper.UI.WPF.Controls.HamburgerMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:SightKeeper.UI.WPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.DataContext>
        <viewModels:HamburgerMenuVM/>
    </UserControl.DataContext>
    
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0">
            
            <Grid.RowDefinitions>
                <RowDefinition Name="ToggleExpandButtonRow" Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <ToggleButton Name="ToggleExpandButton" Grid.Row="0" Style="{StaticResource MaterialDesignHamburgerToggleButton}"/>
            
            <ListBox Name="SideMenu" Grid.Row="1"/>
            
        </Grid>
        
        
        
        <ContentControl Name="MainContent" Grid.Column="1"/>
        
    </Grid>
</UserControl>
